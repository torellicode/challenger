<!DOCTYPE html>
<html>
  <head>
    <title>Admin Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100">
    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="bg-indigo-800 w-64 space-y-6 py-7 px-2 absolute inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition duration-200 ease-in-out">
        <nav class="space-y-3">
          <div class="px-4 py-2.5">
            <span class="text-white text-lg font-semibold">Admin Dashboard</span>
          </div>
          <%= link_to admin_dashboard_path, class: "block px-4 py-2.5 text-gray-200 hover:bg-indigo-700 rounded-lg #{current_page?(admin_dashboard_path) ? 'bg-indigo-700' : ''}" do %>
            <span>Dashboard</span>
          <% end %>
          
          <%= link_to admin_users_path, class: "block px-4 py-2.5 text-gray-200 hover:bg-indigo-700 rounded-lg #{controller_name == 'users' ? 'bg-indigo-700' : ''}" do %>
            <span>Users</span>
          <% end %>
          
          <%# Add more nav items here as features are added %>
        </nav>
      </div>

      <!-- Main Content -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top bar -->
        <header class="bg-white shadow">
          <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8 flex justify-between items-center">
            <h1 class="text-2xl font-semibold text-gray-900">
              <%= content_for?(:header) ? yield(:header) : 'Dashboard' %>
            </h1>
            <div class="flex items-center">
              <span class="text-gray-500 mr-4"><%= current_user.email %></span>
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-sm text-red-600 hover:text-red-800" %>
            </div>
          </div>
        </header>

        <!-- Main content -->
        <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <% if notice %>
              <div class="bg-green-50 text-green-800 p-4 rounded-lg mb-6">
                <%= notice %>
              </div>
            <% end %>
            
            <% if alert %>
              <div class="bg-red-50 text-red-800 p-4 rounded-lg mb-6">
                <%= alert %>
              </div>
            <% end %>

            <%= yield %>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
