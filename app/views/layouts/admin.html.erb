<!DOCTYPE html>
<html class="h-full bg-gray-100 sm:bg-blue-800">
  <head>
    <title>Admin - Challenge</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="h-full">
    <div class="h-full flex flex-col">
      <div class="sticky top-0 z-50">
        <%= render "shared/nav" %>
      </div>

      <%# Main Content Area %>
      <div class="flex-1 flex flex-col overflow-hidden">
        <%# Header %>
        <div class="flex-none bg-blue-800 text-white">
          <div class="py-4 px-4">
            <h1 class="text-2xl font-semibold">
              <%= content_for?(:header) ? yield(:header) : 'Dashboard' %>
            </h1>
          </div>
        </div>

        <%# Content Area %>
        <div class="flex-1 flex overflow-hidden">
          <%# Sidebar %>
          <div class="flex-none">
            <%= render "shared/sidebar" %>
          </div>

          <%# Main Content %>
          <div class="flex-1 overflow-y-auto p-4 bg-gray-100 sm:rounded-tl-lg">
            <%# Flash Messages %>
            <%= render "shared/flash_messages" %>

            <div class="flex-1 h-full">
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
